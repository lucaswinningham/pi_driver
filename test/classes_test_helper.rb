require 'test_helper'

class ClassesTest < TestCase
  def setup; end

#   def teardown
# p ObjectSpace.each_object(PiDriver::Utils::Interrupt).count
#     ObjectSpace.each_object(PiDriver::Utils::Interrupt).each { |interrupt| interrupt.clear }
#   end
end
